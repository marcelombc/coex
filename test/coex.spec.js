import chai from 'chai';
// import Coex from '../src/Coex';

chai.expect();

const expect = chai.expect;

describe('coex', () => {
    it('first test', () => {
        expect(1).to.be.equal(1);
    });
});
